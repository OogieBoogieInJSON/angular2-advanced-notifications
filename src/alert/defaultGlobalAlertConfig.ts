import { GlobalAlertConfig } from './globalAlertConfig.type';

let defaultGlobaAlertConfig: GlobalAlertConfig = {
  removeLastIfViewportOverflow: false,
  customContainerClasses: 'anAlertItem__item--theme-material'
};

export default defaultGlobaAlertConfig;
